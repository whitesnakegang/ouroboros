openapi: "3.1.0"
info:
  title: "OpenAPI definition"
  version: "v0"
components:
  schemas:
    Address:
      type: "object"
      properties:
        roadname:
          type: "string"
        dong:
          type: "string"
        gu:
          type: "string"
    User:
      type: "object"
      properties:
        name:
          type: "string"
        age:
          type: "integer"
          format: "int32"
        height:
          type: "number"
          format: "double"
        address:
          $ref: "#/components/schemas/Address"
    Book:
      type: "object"
      properties:
        title:
          type: "string"

