{
  "path": "/api/users",
  "method": "GET",
  "summary": "사용자 목록 조회",
  "description": "모든 사용자 목록을 조회합니다",
  "deprecated": false,
  "tags": ["User"],
  "parameters": [],
  "responses": {
    "200": {
      "name": "OK",
      "description": "성공",
      "content": {
        "application/json": {
          "schema": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "mockExpression": "{{$person.fullName}}",
                "description": "이름"
              },
              "age": {
                "type": "integer",
                "mockExpression": "{{$number.int(min=1, max=100)}}",
                "description": "나이"
              }
            },
            "required": ["name", "age"],
            "orders": ["name", "age"]
          }
        }
      },
      "headers": {}
    }
  }
}